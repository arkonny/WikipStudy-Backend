type Result {
  quiz: ID!
  user: ID!
  score: Int!
}

type Query {
  resultsByQuiz(quizId: ID!): [Result]
}

input AnswerInput {
  quizId: ID!
  answers: [String]!
}

type Mutation {
  answerQuiz(answer: AnswerInput!): Result
}
