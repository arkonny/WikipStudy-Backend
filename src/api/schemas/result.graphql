type Result {
  quiz: ID!
  owner: ID!
  score: Int!
}

type Query {
  resultsByQuiz(quizId: ID!): [Result]
}

input AnswerInput {
  quizId: ID! @constraint(format: "uuid")
  answers: [String]!
}

type Mutation {
  answerQuiz(input: AnswerInput!): Result
}
